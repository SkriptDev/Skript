test "item comparison":
	set {_test} to itemstack of dragon head
	assert material of {_test} is dragon head with "failed to compare against a head"
	set {_test} to itemstack of oak door
	assert material of {_test} is oak door with "failed to compare against a door"
